## Learnings
- When `getdesign` fails, a small Airtable-inspired token file is enough to satisfy the setup task.
- Keeping Copilot instructions aligned with AGENTS.md avoids drift between local and repo guidance.
